<!-- res/values/styles.xml -->
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@color/windowBackground</item>
        <item name="android:fontFamily">@font/myfont_regular</item>
        <item name="fontFamily">@font/myfont_regular</item>
    </style>

    <style name="RoundedButtonStyle">
        <item name="android:background">@drawable/block_background</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:layout_margin">8dp</item>
        <item name="android:fontFamily">@font/myfont_bold</item>
        <item name="fontFamily">@font/myfont_bold</item>
    </style>

</resources>